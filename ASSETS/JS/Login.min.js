

class Login
{
    operator()
    {
        return 'operator.php';
    }
    doAuth(param)
    {
       var Response = net.Post(this.operator(),param).trim();
       console.log(Response);
       if(Response == "200")
       {
           window.location.href = 'index.php';
       }
    }

}
class Network
{
    Post(addr,param)
    {
        var content =  $.ajax({
            url:addr, type:"POST", data:param, cache:false, async:false,success:function(result)
            {
                
            },
        }).responseText;
        return content;
    }
}

var login = new Login();
var net = new Network();
 